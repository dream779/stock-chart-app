# Anthropic-compatible base URL for MiniMax
ANTHROPIC_BASE_URL=https://api.minimaxi.com/anthropic

# MiniMax API key (server-side only; never expose to the browser)
ANTHROPIC_API_KEY=<your-minimax-api-key>